---
description: Quick question, outside the pipeline — no handoff doc, no ceremony
agent: ask
---

<question>
$ARGUMENTS
</question>

Answer directly. If this turns out to need real design thought rather than a
quick answer, say so and point back to `/plan` instead of attempting it here.
